import { FeatureInfo } from './feature-info';

describe('FeatureInfo', () => {
  it('should create an instance', () => {
    const directive = new FeatureInfo();
    expect(directive).toBeTruthy();
  });
});
